# Netlify Configuration
# https://docs.netlify.com/configure-builds/file-based-configuration/

[build]
  command = "pnpm build"
  publish = "dist/client"
[dev]
    command = "pnpm dev"
    port = 3000